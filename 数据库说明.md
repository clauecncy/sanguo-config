# 武将战法库存数据库

## 文件与范围

- SQLite：data/三国谋定天下_Steam_S1.sqlite3（保留旧文件名，当前账号S2）。
- 当前库存种子：data/account_inventory.json。
- 结构：data/schema.sql；离线维护：scripts/account_inventory.py；校验：scripts/verify_database.py。
- 可读清单：武将战法库存.md；缺口：data/覆盖率报告.md。
- 全局参考定义与账号拥有分开。仅account_generals/account_tactics及v_owned_*表示本次账号库存，不用全局表行数当拥有数量。

## 账号字段

account_generals以(general_id,variant)为键：44名普通常驻金将，另有英雄张梁限时快照。advancement为红度，availability区分常驻与快照；不会因为同名把3红英雄覆盖2红普通张梁。截图剩余15小时16分但拍摄时刻未知，不推算绝对到期时间，也不默认该英雄现在仍有效。

account_tactics保存62个金/紫战法。advancement=NULL为未知；advancement_verified_at和advancement_source保留红度证据。30项金色战法沿用2026-09-02的红度，本次未重新核定；破军袭敌和31个紫色战法红度未知。

账号等级、编队、携带者不维护，兼容字段为空。紫色武将已从数据库定义及关联记录删除；原始快照、历史活动与notes不删除。

## 50级默认四维

v_owned_generals显示50级未加点估算：四舍五入(5级基础值+公开成长值×45)。44名普通金将四维齐全；成长精度有限，约±1误差。没有计入等级50点自由属性、每红10点自由属性、装备或战斗增益。英雄版不套普通版四维，当前为空。

generals仍保留5级基准与成长作为推算依据，而不是账号当前等级。general_stat_observations中的50级实测和实际加点保留用于核验。

## 满级战法

tactic_level_observations仅导入10级，种子为data/tactic_level_observations.json。tactics.description_level=10才表示可信满级正文；缺少依据时description_raw=NULL，v_owned_tactics.detail_status显示缺口。

原库正文混有1级及1至2级预览，不能整体标记满级；已从当前正文与tactic_effects引用中移除。历史网页快照留作原始资料，不参与满级计算。不会用乘二、取区间上限等方法猜测10级效果。

当前保留6项Steam满级详情，其中4项属于账号库存：束手无策、横征暴敛、固若金汤、谈笑诛心；料事如神、上智为间仅为全局参考。其余58个已有战法满级详情未核定。词典effects保留，已核正文的状态关系重新建立。

## 查询

```sql
SELECT * FROM v_owned_generals WHERE availability='常驻' ORDER BY faction,name;
SELECT * FROM v_owned_generals WHERE name='张梁';
SELECT * FROM v_owned_tactics ORDER BY quality,name;
SELECT name FROM v_owned_tactics WHERE advancement IS NULL;
SELECT name, max_level_effect, detail_source
FROM v_owned_tactics WHERE description_level=10;
```

## 更新与验证

```powershell
& 'C:\Users\bixianjue\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -X utf8 scripts/account_inventory.py
& 'C:\Users\bixianjue\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -X utf8 scripts/verify_database.py
```

离线更新不联网，会先在notes/backups/2026-09-08-1800/data/保存首次修改前SQLite（不覆盖），再事务化同步种子、移除紫将、应用满级口径并生成清单。校验包括完整性、外键、逐项库存红度、50级四维、重复导入和空库重建。

scripts/sync_game_data.py仍为显式网络全量重建入口；重建后同样叠加新库存与满级口径，不再强制S1。历史v_s1_*视图只是S1筛选，不是当前账号查询入口。

## 回滚

用本次Git实现提交的反向提交恢复源码和数据；本地修改前SQLite备份位于notes/backups/2026-09-08-1800/data/三国谋定天下_Steam_S1.sqlite3。不删除notes、原始截图或历史参考。
