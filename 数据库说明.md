# Steam S1 配队数据库

## 文件

- SQLite 数据库：`data/三国谋定天下_Steam_S1.sqlite3`
- 数据模式：`data/schema.sql`
- 网络规范化快照：`data/snapshots/sgmdtx-latest.json`
- 当前账号库存种子：`data/account_inventory.json`
- 同步脚本：`scripts/sync_game_data.py`

## 数据层级

1. `Steam已核`：用户当前 Steam S1 客户端截图或面板，冲突时优先。
2. `S1参考`：网络资料中明确标记 S1 的武将、战法与缘分数据。
3. `待可靠来源`：当前没有可核验全量来源的字段，例如部分武将的韬略推荐。保留空值，不用攻略印象填充。

韬略采用五层结构：`strategy_books` 保存定义，`strategy_rules` 保存已知规则，`general_strategy_eligibility` 保存每名武将预览页中的可能获得列表，`account_general_strategy_slots` 保存账号实际三槽，`general_strategy_recommendations` 保存理论推荐。实际结果不能由理论推荐反推，也不能把逐武将候选列表擅自归纳成职业公共池。

配队只使用三个过滤视图：`v_s1_gold_generals`（S1金将）、`v_recommendable_tactics`（已确认S1且金/紫战法）、`v_recommendable_strategy_books`（紫色或专属韬略）。其他品质仅保存在全量归档中。

数据库同时保存全站武将和赛季标签。当前账号、S1 武将自带战法和第三方 S1 图鉴中的战法进入 `v_s1_tactics`；金/紫且不是武将自带战法的条目才进入 `v_recommendable_tactics`。第三方图鉴只用于补充品质、定位和 S1 候选范围，Steam 截图冲突时优先覆盖。战法保留原始完整说明；`tactic_effects` 将说明中出现的会心、洪水、震慑等状态关联到 `effects` 的精确定义。

## 属性计算

资料站的基础四维是 5 级基准。公开成长值仅显示两位小数，目标等级未加点面板的**估算式**为：

```text
估算四维 = 四舍五入(5级基础值 + 显示成长值 × (目标等级 - 5))
```

再叠加等级自由点、升阶自由点和实际分配。等级每到 10 的倍数获得 10 点自由属性，50 级共 50 点；每升一阶再获得 10 点自由属性。由于客户端可能使用未公开的更高精度成长值，公开数据推算结果可能相差 1 点；涉及先手阈值时必须优先使用 `general_stat_observations` 的实测面板。缘分、阵型、韬略和战法的战斗内增减另算，不能直接混入裸面板。

## 常用查询

```sql
-- Steam S1 全武将及其 50 级未加点四维
SELECT * FROM v_general_level50_estimate
WHERE name IN (SELECT name FROM v_s1_generals)
ORDER BY initiative DESC;

-- 当前账号武将及网络基础数据
SELECT g.name, a.level, g.faction, g.troop_type,
       g.base_force, g.growth_force, g.base_initiative, g.growth_initiative
FROM account_generals a JOIN generals g ON g.id = a.general_id
ORDER BY a.level DESC, g.name;

-- 战法正文及其状态定义
SELECT t.name, t.description_raw, e.name AS effect, e.exact_effect
FROM tactics t
LEFT JOIN tactic_effects te ON te.tactic_id = t.id
LEFT JOIN effects e ON e.id = te.effect_id
WHERE t.name = '决水破敌';
```

## 更新

```powershell
& 'C:\Users\bixianjue\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' scripts\sync_game_data.py
```

同步会重建数据库和网络快照，再叠加 `account_inventory.json` 与 Steam S1 固定覆盖。新增账号武将、战法或升阶后，应先更新库存种子再同步。

## 战法等级实测

`tactics.description_raw`保留资料站参考等级正文；游戏内指定等级截图写入`tactic_level_observations`，避免用满级数值覆盖低等级资料。

```sql
SELECT t.name,o.level,o.activation_rate,o.effect_raw,o.effect_json,
       o.verification_status,s.url AS source_url
FROM tactic_level_observations o
JOIN tactics t ON t.id=o.tactic_id
JOIN sources s ON s.id=o.source_id
WHERE t.name IN ('料事如神','上智为间','束手无策','横征暴敛','固若金汤','谈笑诛心')
ORDER BY t.name,o.level;
```

可重复同步的截图实测种子位于`data/tactic_level_observations.json`；`scripts/sync_game_data.py`重建数据库后会自动恢复这些记录。
