# 演武任务

存在 current_state.json 时，先读它作为本期唯一当前状态；当前仓库.json、当前状态.md 是生成投影。decision_history.json 和旧轮次建议只供追溯，不能当当前推荐。资料同步及状态更新使用 [统一材料入口](../materials.md)，不用日期硬编码脚本。公共机制可定向查 game/facts.json。

2026-09-24用户确认本期续战：单场最多8回合；平后保留结束兵力和武将存活状态，重置增减buff后继续战斗。复盘可按大幅兵力差及存活状况判断胜负倾向，不必把明显优势/劣势一律评价为中性平局。保存界面原始结果和分析判断两套字段，未展示的续战不伪造为实见结果；不把首场叠满属性直接带入续战分析。

用户曾允许在技能/演武证据已核对并写入MD后清理图片材料。历史原图不在项目中不自动否定当时由用户确认的MD；按MD实际记载的机制、等级、数值与确认来源逐字段使用，不把概述扩展成完整10级效果。先找已确认MD和结构化记录，只有关键字段仍缺失或冲突时再定向询问。该历史清理许可不等于本期新图已核完即可立即删除；本期材料按对应任务的保留规则处理。

bixianjue本次`s2-2026-09-23`连续演武的确认方式（用户明确，2026-09-23）：助手明确推荐某组后，用户未反驳而直接发送下一轮选卡截图，即视为上一轮按推荐组实际选择。用户明确改选时以其实际选择为准。此约定仅限该账号该期连续任务，不跨期、跨账号自动套用；写入时保留由哪一轮新截图触发确认。

每轮新材料先逐项查公共库的实体、定义、等级观察、可信度及来源，不把“库中有文字”误当成本期可信效果，也不在库已有可信详情时重复要求用户拍图。用户提供的新增武将、战法或完整详情若公共库尚无对应实体或可信字段，按公共数据工作流同步公共库、SQL基线与来源索引；截图只显示卡名、角色或百分比时，不凭空补技能正文。库存归属和演武选中状态仍只留个人期次，不写公共库。

强制证据门槛：先读公共勘误 `game/rules/技能勘误与证据优先级.md`，再查数据库和来源。查到旧网页不等于本期核实；关键效果仍不确定或冲突时先问用户，不猜测、不编造、不依据该效果给确定推荐。缴械/震慑/技穷不得互换，未核低等级资料不得外推满级。舍生取义按2026-09-16用户详情确认为自身缴械2回合，旧快照“震慑”错误。用户要求永久保留此工作习惯。

技能核对顺序：先查公共定义及等级观察，再定向检索game/sources中的原始网页快照、来源索引和公共机制资料。主表正文为空不等于没有资料；整理文本漏写效果不等于版本冲突。选卡摘要、角色/适配提示只能定位问题，不能代替完整技能。区分机制与等级数值、主动治疗与治疗增益；仍有关键缺口时向用户索取详情，不因未查全就猜测并给出确定排序。低等级机制可作有来源的机制参考，不能外推10级数值。

重随硬限制（用户明确纠正）：武将重随与战法重随的材料均必须同时满足金色品质、非支援身份。紫色不允许重随。推荐材料前逐项核对品质、来源、占用与替换代价，不将未知当成符合；战法支援锦囊增加紫色位，不能与重随条件混淆。

固定候选（用户2026-09-16确认，S1/S2通用）：最后一轮锦囊恒为武将重随、战法重随、战法支援（新增紫色战法支援位）；可选阵型恒为一字、箕形、雁形、方圆、锥形、鱼鳞。后续不再要求用户重复提供候选名称。阵型效果仍查公共formations及来源，固定名称不等于用户重新确认了跨赛季效果数值。

用户明确偏好：演武任务默认不执行git commit或git push，只有用户主动明确要求时才执行对应操作；本地提交授权不等于推送授权。修改前使用项目内notes/backups保护相关既有文件，不撤销历史提交。

必须明确账号及期次。用户只说“本期”时，读取该账号期次索引提出具体期次并确认，不跨账号或自动复用上期仓库。

```powershell
python scripts/event.py --user bixianjue --event s2-2026-09-02
```

只读取该期 `event.json` 和与本次问题有关的记录；需要支援核对时才读取该账号常规库存。公共规则按平台、赛季和本期截图核对。

每期保存原生仓库、逐轮选卡、支援、方案、战报和证据。新阶段留在同一期。用 `status` 区分建议、已确认和已撤回，未知不得默认确认；历史文件保持可追溯。

截图原图默认只保留本地。需同步文字证据时，在期次目录保存截图转译Markdown；战报、选卡与阵容截图按原图SHA-256逐张锚定，并在`证据清单.json`记录`transcription_ref`、`transcription_status`、可信字段和可信原因。`IMAGE_TEXT_TRANSCRIPTION_V1`标记用于核对原图身份与转译记录的关联，不代替准确性复核。聚合或连续多图证据要明确标成组级汇总，不把累计数据分摊给单张；分析判断、未展示字段和锁定状态不得混写为截图事实。

新建一期使用 `赛季-首次记录日期`，实际开赛日期未知时留空。不得从文件日期推算开赛时间。演武资源不能反写常规库存。
