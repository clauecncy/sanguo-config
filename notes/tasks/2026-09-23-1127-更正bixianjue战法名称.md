# 更正 bixianjue 战法名称

## 1. 原始需求

用户指出新增战法应为“忘私相助”“计袭粮仓”，而非此前识别的“定心相助”“计策粮仓”，并询问“未收录公共定义、无实体ID、待补录”的含义。

## 2. 我的理解

本次更新用户沿用本连续任务明确的 `bixianjue`。删除两条误识别的个人库存战法记录，以用户确认的正确名称替换；六张当前截图不变，其他武将、战法和字段不动。核对正确名称在公共库的身份，澄清实体匹配与效果可信度是两回事。

## 3. 执行步骤

1. 确认工作区和上一任务记录，读取指定账号库存及库存工作流。
2. 按正确名称定向查询公共战法库，不以模糊匹配或截图猜字决定身份。
3. 通过指定账号增量入口删除两个错名、加入两个正名，并更新等级说明。
4. 校验库存、派生报告、实体ID和六张现有截图未改变。
5. 修正当前业务逻辑，给上一任务记录添加更正提示，追加变更记录并本地提交。

## 4. 涉及的业务逻辑

个人库存按战法标准名称匹配公共实体；用户明确纠正名称时优先采用用户确认。`entity_id` 用于连接公共定义，不能自行生成。公共表中 `trust_status=需要确认` 仅表示效果正文等字段尚未由可信来源完整核定，不等于实体不存在，也不等于用户未拥有。等级10只按本批截图记录；未显示的红度继续未知。

## 5. 修改前状态

- 工作区干净，基线提交：`affa674c6dfc78499ad1bf0b85d633f0ff4d90a3`。
- 指定账号库存44条武将、65条战法（34金31紫）；两条错名金色战法为10级但 `entity_id: null`，库存校验将它们列为公共定义待补录。
- 公共库准确收录 `忘私相助`（金色、被动、`tactic:08ea3238229b37e68042`）与 `计袭粮仓`（金色、追击、`tactic:1ebe1c66d527d7992788`）。两条公共记录的满级效果正文仍标需要确认，和是否有实体ID是不同问题。
- `evidence/current/` 保留六张原始截图及SHA256清单；本次没有新图。

## 6. 关键决策

只改两条库存名称及其派生记录，不改公共库原始来源、信任状态、其他账号或旧期资料。沿用原图的10级和金色品质，红度不填0；不用 `--images`，确保当前证据批次不被轮换。

## 7. 具体修改点

- `user/bixianjue/inventory.json` 精确删除误写的定心相助、计策粮仓，新增忘私相助、计袭粮仓；两条仍为截图所示金色、10级，红度未显示且保持未知。按正确名称自动匹配公共实体ID，并同步修正 `tactic_level_note`。
- 重新生成 `user/bixianjue/reports/库存.md` 和 `覆盖率.json`，覆盖率中的 `unresolved` 由两项变为空列表；44条武将、65条战法及其他记录不变。
- `notes/业务逻辑.md` 改为正确的当前战法名称；在上一任务记录顶部追加更正提示并校正截图哈希映射，保留首次误识的执行历史。当前六张库存证据原图和 `manifest.json` 未修改。

## 8. 验证结果

- `python scripts/account_inventory.py --user bixianjue validate` 与 `report` 均通过：44条武将、65条战法，`unresolved: []`。
- 忘私相助匹配 `tactic:08ea3238229b37e68042`，计袭粮仓匹配 `tactic:1ebe1c66d527d7992788`；错名在当前库存和报告中均不存在。
- 两项仍为10级、红度未知；其余库存未变。`git diff --exit-code -- user/bixianjue/evidence/current` 确认本批六张图及清单未改变，`incoming/` 已清空。
- 公开库的两条满级效果正文仍标“需要确认”，与实体是否存在无关；本次未修改公共库或其他账号。

## 9. 产出物

- 当前唯一库存源 `user/bixianjue/inventory.json`，派生报告 `user/bixianjue/reports/库存.md` 和 `覆盖率.json`；图片证据仍在 `user/bixianjue/evidence/current/`。
- 本地实现提交号：`a34875d4d62df8ae752d267b4e2586052e01cf03`，已记入 `notes/变更记录.md` 本次更正条目；没有远程操作。

## 10. 回滚方法

核对实现提交 `a34875d4d62df8ae752d267b4e2586052e01cf03` 后对其创建反向提交，仅撤销本次名称修正和文档记录；保留此前正确的武将删改、其余战法、原始截图、`notes/` 与 `.git/`。
