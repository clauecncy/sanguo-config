# 公共材料同步与结构审计

## 1. 原始需求

核实演武获取的武将战法材料是否同步通用层并标可信来源，分析项目缺漏与优化项，只列举，不实施迁移。

## 2. 理解及范围

确认根目录D:/workspace/sanguo-config；审计本期s2-2026-09-23的直接技能证据以及公共数据模型、验证入口、事件状态和来源链。没有逐条重新审核全项目所有历史OCR，不把当前抽查扩大为历史完整性保证。

## 3. 执行步骤

读取固定资料、工作流、公共查询及可信度程序、测试和事件JSON；只读检查SQLite/SQL一致性及完整性；逐项核对27张2026-09-23/24公共证据图的来源、等级观察、索引和哈希；在内存库重放旧可信度分类；运行32项测试与verify_project；检查事件证据遗漏与当前状态冲突。

## 4. 业务口径

完整技能图可确认对应技能字段，不认证整个武将基础四维；战报、增减buff属性只进事件层是正确边界。用户口述和已确认MD需要可复用的字段级事实记录，但不冒充截图完整10级。红度变体必须与基础机制和白板数值分开。

## 5. 核查事实

- 本次公共27图覆盖25个不同技能，全部有sources来源、可信10级观察、evidence-index记录且SHA256匹配；这25项主记录均可信。27包括历史原图复核和同技能不同红度/重复复核，不等于27个技能。
- 25项：乱世奸雄、五雷轰顶、以静制动、兵贵神速、划湘分荆、勇冠贲育、固镇襄樊、奇正相生、奇门遁甲、屯田令、忘私相助、悲愤诗、披坚执锐、指点乾坤、摧坚克难、断敌粮道、来好息师、横扫千军、破阵驰围、裸衣血战、诡道玄机、铸甲销戈、陷阵蹈难、韬光养晦、风卷残云。
- SQLite integrity_check=ok，SQL独立内存重建与当前库相同。
- 公共全库：战法57可信/175需要确认，等级观察80可信/3需要确认；武将主定义102需要确认、四维观察2可信。不代表102武将都没有已核技能，而是主定义与自带技能可信范围不同。
- 本期event.skill_evidence仅列6图，不完整；证据/贾诸法配置参考.png未列本期证据清单。这是索引遗漏，不是图丢失。
- 32项单元测试通过；verify_project失败，1117条断链全部来自notes/backups，活跃目录中该检查发现0条断链。

## 6. 问题与优先级

### P1 可信度重算可反转人工勘误

scripts/catalog_trust.py:6硬编码披坚、五雷、断敌为残缺；classify每次覆盖人工状态。内存重放确认三项新可信主记录被降级；韬光旧观察27漏“自带”却又被标可信。新旧两条同时可信会再次导致错误配将。建议废止全量覆盖式推断，新增人工核定锁、supersedes/retracted字段，按具体来源而非名称维护残缺；增加回归测试。正式库未运行重算。

### P1 红度上下文未进入主查询选择逻辑

scripts/project_data.py:243直接将主表description_raw返回为max_level_effect；244仅依10级和可信给detail_status。铸甲主正文70.85%是三红，来好95.4%是二红，主表没有对应红度列；目前只在trust_reason及观察保存。建议以等级/红度/平台/赛季/战斗场景选择观察，机制与数值拆分，无对应红度返回未知而非借用其他红度。支援继承红度不等于所有演武原生战法同红度。

### P1 通用机制缺少统一的已核事实入口

徐黄瑜借刀链、荀彧名称纠正等仍主要在本期阵容链条纠正.md；临机制胜、天香、木牛流马、苦肉计主表仍为低级旧网页且需要确认。不能仅凭有MD就把这些全升可信，但应提升已确认的单独机制到通用事实层并引用用户说明/MD。支援继承、8回合和buff重置跨event/workflow/rules多处重复；支持红度的旧公共规则其实已有记录，分散导致反复追问。

### P2 当前状态与历史建议混存

第7轮锦囊建议JSON顶层还留郭嘉/谋略、support_status仍是水淹；当前仓库及新快照已是清风，文字status要求读者自行判优先级。notes/业务逻辑也包含旧“建议交换奇正韬光”等被撤回内容。建议事件单一current_state，推荐历史单独append-only记录，显式status/superseded_by，文档从当前状态生成。

### P2 索引和证据关联不完整

公共图本身链路核查通过，但event技能索引6图及贾诸法参考图漏清单影响跨对话发现。建议每份证据统一ID，关联实体、字段、事件、用途、核定和撤回信息；跨目录引用/生成索引，避免每次手工追加多个表。

### P2 一次性写入脚本绕过统一流程

scripts中18个202609日期脚本，多个直接写死bixianjue及event，违背入口应显式--user/--event的规则；共同helper硬编码2026-09-23，后来脚本再手动纠正日期。部分脚本先复制证据或更新事件，再写公共库和多份JSON，故障时可能部分成功，备份不等于自动事务回滚。建议建立统一导入命令、显式参数、dry-run、幂等证据ID、预校验及分阶段提交/恢复；旧一次性脚本应归档并禁止误重跑，未经授权不删除。

### P2 验证器漏项与备份误报

verify_project.py:40递归扫描备份却仅排除notes/tasks，导致1117条备份相对链接误报，正常验收被噪声阻断。validate_public主要检结构完整和ID，不检观察红度、来源哈希、勘误优先级、当前状态一致性。32测试通过不足以覆盖上述实际问题。建议把当前工作区与历史备份校验分开，加证据、分类重放、红度选择、已撤回推荐、战报续战去重测试。

### P3 文档和职责进一步收敛

docs/data-model.md对新增trust/screenshot_verified_fields、红度观察和战报快照描述不足；项目说明仅跳转入口。建议明确game定义/观察/事实、references阵容知识、user个人事实、notes审计历史四层，并文档化字段与读取顺序。不建议为此马上拆出大量服务或新增数据库。

## 7. 本次修改点

仅新增此审计任务报告并追加变更记录，未修复/重构数据库或业务脚本，未改库存或配置。内存复现实验不写回。

## 8. 验证结果

上述27图来源哈希/SQL完整性通过；32测试通过；verify_project失败根因已定位。此审计并非全历史材料逐字段复核，无法证明所有过去口述/图片均已同步。

## 9. 产出与建议顺序

本报告为交付。优先处理可信度回退、红度查询、当前状态入口；其次统一导入及证据索引/校验，最后整理文档与旧脚本。用户仅要求分析，未实施修复、无commit/push。

## 10. 回滚

notes/backups/2026-09-24-1530-structure-audit/notes/变更记录.md为变更记录修改前副本；核后续变化后恢复。审计报告为永久任务资料，不视为临时内容。
