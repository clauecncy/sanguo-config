# 项目分层与账号隔离

按用户确认方案拆分公共游戏数据、赛季参考和个人库存；账号为 bixianjue、zhaoguohua。演武按账号、期次隔离。完整迁移清单见 `docs/migration-manifest.json`。

- 公共库移除账号表；个人库存唯一编辑来源为 user/<账号>/inventory.json。
- 全局只统计金将及金紫战法；bixianjue 保留 44 常驻金将、1 英雄快照、62 战法；zhaoguohua 保留 46 金将、48 战法，6 紫将归档。
- 每次库存更新必须明确用户，缺少时先询问，脚本没有默认用户；部分输入保留未涉及的原记录。
- AGENTS.md 与独立工作流限定任务读取范围，综合长文移到历史归档。
- 两库的 42 项公共记录差异保留在冲突清单，未以旧值覆盖主库。
- 已迁移 111 个文件并保留原校验值；旧参考文档存在 51 处未随仓库同步的源图/OCR图片引用，已明确标识原始缺失，没有伪造图片。
- 旧联网和日期专用脚本归档并禁用执行；新的公共同步入口支持已保存基线离线重建，禁止覆盖不同的新版本或删除用户引用实体。
- 迁移原 stash 保留，不混入旧改动。

验证：库存逐字段对比、数据库完整性与外键、迁移文件校验、当前链接检查，以及自动测试（部分更新、重复导入、账号隔离、参数拒绝、英雄/未知值、演武身份、离线重建和最小读取路由）。

最终结果：20 项自动测试全部通过；`verify_project.py --migration` 通过。修复 Windows SQLite 连接释放问题，并验证新战法红度使用本次证据日期，不继承旧批次日期。

后续正常维护运行 `python scripts/verify_project.py`；此次原始迁移验收另加 `--migration`，避免把未来合法库存变动误判为迁移丢失。
