# transport 故障接续核验

## 1. 原始需求
旧任务发生 transport 故障；不依赖旧线程状态，以工作区实际文件检查已完成、遗漏，保留正确修改并继续验证。

## 2. 理解
项目根目录 D:/workspace/sanguo-config。未读取旧线程；按磁盘审计报告、材料说明、迁移配方、事务日志及源码确定接续范围为公共材料与当前状态结构修复。不是新库存更新，也不推断新的游戏选择。

## 3. 执行步骤
检查 git status、diff、最近提交、固定文档与相关任务；阅读新增材料模块和测试；重新运行原48项测试及全项目校验；检查迁移幂等性；修复导入判重和恢复预演；追加回归测试并全量复验。

## 4. 业务逻辑
证据身份按内容SHA256和技能、等级、红度、平台、赛季、场景判重，而不是来源记录名称/日期。相同证据上下文的冲突转写拒绝写入；已核定状态不被重复导入重置。恢复预演执行与实际恢复相同的文件哈希、备份完整性和后续编辑冲突检查，但不写文件。

## 5. 修改前状态
工作区已有大量未提交的9月23日至24日演武证据与结构迁移修改。evidence-state-v2迁移标记存在，18:13事务日志status=committed；此值表示文件事务完成，不是Git提交。原48项测试和verify_project重新执行通过。迁移预演changed_files为空。固定文档已更新，但结构实施完成记录缺失。

## 6. 关键决策
不重放迁移，不恢复整个工作区，不修改公共数据库、个人库存、截图或当前阵容。已有实现包括人工核定保护、红度查询、8项机制事实、统一证据索引、单一当前状态、18个日期脚本停用归档、事务发布及验证器。保留现状，只补查明缺口。涉及演武接续且工作区混有历史未提交资料，按演武工作流保留不自动commit/push；本次用项目内备份保护。

## 7. 具体修改
- scripts/materials.py：同证据按内容ID及上下文查询全部已有观察；不因来源日期变化制造重复来源/观察；冲突转写拒绝。recover预演调用实际检查逻辑。
- scripts/material_transaction.py：rollback新增dry_run参数，完成所有前置检查后无写入返回。
- tests/test_materials.py：补跨来源日期幂等、既有截图冲突拒绝、新证据首次导入及重复发布、恢复预演与后续编辑冲突测试；隔离夹具复制机制事实引用文件。
- notes/变更记录.md与本记录：补接续结果和恢复边界。

## 8. 验证结果
最终52项单元测试全部通过。新增测试开发时发现隔离夹具缺少机制事实引用文件，补齐后通过，并非正式资料丢失。verify_project通过，SQLite与SQL重建、观察JSON、证据哈希、状态投影一致；96条公共证据索引、8项机制事实、1个当前状态。战报保持实见11胜4负3平，倾向合并12胜6负。断敌一红查询返回观察65的113.3%，无零红混用。迁移预演零变更；原事务恢复预演通过且未执行恢复。git diff --check通过（仅已有LF/CRLF提示）。验证器报告的51项历史原始材料缺失为迁移清单既有记录，不宣称已恢复原图。

## 9. 产出物
修复后的材料入口、事务模块、52项测试及本任务记录。无新游戏数据导入，无删除，无远程操作。本地Git提交号：无；HEAD仍为8d506c8，未把其他历史资料混入提交。

## 10. 回滚方法
本次修改前4份既有文件按原路径保存于notes/backups/2026-09-24-1826-transport-continuation。需要撤回本次补丁时先核对后续编辑，只恢复对应源码、测试和变更记录；保留本任务记录、原图、旧备份和此前结构迁移。不要对已完成的18:13迁移执行整批回滚来撤销本次代码补丁。

## 当前磁盘复验补记（2026-09-24 19:58）
- 按当前工作区重新运行完整测试：53项通过；`verify_project.py`通过，公共SQLite完整性为ok，96条证据、8项机制事实、1个当前状态；迁移预演仍为`changed_files: []`；`git diff --check`通过。复验未改变工作区状态。
- 当前重新执行18:13事务恢复预演时，系统按设计拒绝恢复：`game/sources/evidence-index.json`已在事务完成后编辑。对照事务staged文件，96条记录路径与数量相同，33条仅将availability从`present`改为`local_only`；当前索引及其修改前副本仍在`notes/backups/2026-09-24-before-push-exclude-transcribed-images/`。这是后续编辑冲突保护，不是迁移残留；不得对该事务执行恢复或覆盖这批状态。
- 原记录中的“恢复预演通过”描述的是当时状态；以当前磁盘再次核验的结果为准。没有应用恢复、没有改动游戏数据、库存或证据图，也未执行远程操作；其余混合工作区改动保持原样。
