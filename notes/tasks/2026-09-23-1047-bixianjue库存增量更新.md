# bixianjue 库存增量更新

## 1. 原始需求

用户说明武将无其他变化，只删除英雄张梁；六张战法详情图中包含此前标记待确认的战法，其余截图对应新增战法。

## 2. 我的理解

本次更新用户为 `bixianjue`。只移除 `张梁（英雄）`，普通张梁及其他武将保留。前三张图核对已有战法，后三张图新增战法；仅记录截图明确展示的名称、金色品质和10级，不把未显示红度记为0。公共库暂未收录的新增战法在个人库存保留为待补录项，不编造实体ID。

## 3. 执行步骤

1. 检查当前库存工作流、账号资料和库存基线。
2. 核对六张截图、现有战法实体和当前库存字段。
3. 为精确删除单条账号库存增加 `remove` 补丁支持及测试。
4. 通过显式用户入口运行增量更新、库存校验和报告生成。
5. 检查计数、待补录项、截图哈希和原始截图未修改，然后本地提交。

## 4. 涉及的业务逻辑

武将只统计金色；战法统计金色和紫色。`advancement: null` 表示红度未知，不等于0。未出现在图片中的库存项目保留。用户明确要求删除英雄张梁；删除操作必须指定名称和版本，不能以未出现在补丁中表示删除。新实体若公共库无定义，仅凭截图记录明确品质并保留待补录状态。

## 5. 修改前状态

- 工作区干净，修改前基线提交：`57542878f5c4ffa8475870d5017bb40ef0c2b034`。
- 库存文件核定日为2026-09-10，45条武将记录（含普通和英雄张梁）、62条战法记录（31金、31紫）。
- 公共库没有 `定心相助`、`计策粮仓` 两个战法实体。
- 用户原始截图位于系统临时目录；项目 `evidence/current/` 尚无库存截图批次。

## 6. 关键决策

只根据截图明确可见数据更新。红度截图未展示，新增战法红度保持未知。三个新增条目里公共库已有定义的按名称匹配；未收录的两个不伪造ID。公共战法数据库不在本次个人库存修改范围内。

## 7. 具体修改点

- `scripts/project_data.py` 增加精确 `remove` 补丁：武将按名称和版本删除，战法按名称删除；拒绝不存在、重复、同时更新又删除同一身份的请求；支持同步更新 `tactic_level_note`。
- `tests/test_project_data.py` 增加指定武将版本删除与歧义/不存在删除的回归测试。
- `docs/workflows/inventory.md` 记录精确删除语法，仍禁止以省略项表示删除。
- `user/bixianjue/inventory.json` 删除 `张梁（英雄）`，保留普通张梁及其他武将；更新原有披坚执锐、断敌粮道、五雷轰顶当前等级为10；新增金色定心相助、计策粮仓、舍生取义，三项均记录10级，未显示红度保持未知。
- 两个未入公共库的条目保留无实体ID的个人库存记录，并由验证结果标记为公共定义待补录。六张原图原样复制到 `user/bixianjue/evidence/current/`，生成SHA256清单；派生更新 `reports/库存.md` 和 `reports/覆盖率.json`。
- 证据映射：披坚执锐 `d46e9d571e40ea2f4eb3150dcbd5a6f06eaa1c6595bf79cecbdd7ce538c33621.png`；断敌粮道 `ffa13f2c2b6dfca80a03206f583d29e4fa77e15316c56127f5b454c9be8c385e.png`；五雷轰顶 `a6d19a657a0dce1f45962ef06bb31c8ddd6b4d738a4d577704ecfc56471ab6c3.png`；定心相助 `ecf4a39f8d59ef3a144ce02335d971900b0633f1a9cf525d2156fda05cde1e2f.png`；计策粮仓 `06cb852557c17cf7e9c95ca80e49dcd29ffda8452be164f603b67fb59de64a2e.png`；舍生取义 `6b7441ee93cd1b4f3b2c13510f6759d1993f684db6dc5817215c65dfb2c5905d.png`。
- 更新 `notes/业务逻辑.md` 记录当前库存计数和未知字段口径。

## 8. 验证结果

- `python -m unittest discover -s tests -p test_project_data.py -v`：28项测试全部通过。
- `python scripts/account_inventory.py --user bixianjue validate` 与 `report` 均通过，库存为44条武将、65条战法；仅定心相助、计策粮仓两项明确报告公共定义待补录。
- 六张截图的SHA256与用户临时目录原图逐一一致，6/6 PNG均成功解码；英雄张梁已不存在，普通张梁仍在；六项截图战法等级均为10，未知红度未被填为0。
- 未改公共数据库、其他账号、原始截图或演武资料。

## 9. 产出物

- 指定账号唯一库存源：`user/bixianjue/inventory.json`；派生报告位于 `user/bixianjue/reports/`。
- 最新六张图片及清单位于 `user/bixianjue/evidence/current/`；清单SHA256与用户原图核对一致。
- 本地实现提交号：`27d3eb974f3f51d57c47d22ed3ed999919190699`，并已在 `notes/变更记录.md` 登记；不执行任何远端操作。

## 10. 回滚方法

核对后对实现提交 `27d3eb974f3f51d57c47d22ed3ed999919190699` 执行反向提交；只恢复本次涉及的库存、脚本、测试、工作流、项目记录和证据文件，不覆盖后续修改，不删除 `notes/`、`.git/` 或用户原始截图。归档提交只包含项目记录，若需回滚也先核对其确切文件范围。
